//! Preview Agent Workbench Phase 1 remote IPC 계약의 integration test root.

mod preview_bridge_contract {
    mod acl;
    mod busy_cancel_waiter;
    mod dispatch_rules;
    mod live_probe;
    mod mcp_server;
    mod navigation_behavior;
    mod payload_contract;
    mod post_review;
    mod probe_url;
    mod receipt;
    mod registry;
    mod startup_path;
    mod toolbar;
}
