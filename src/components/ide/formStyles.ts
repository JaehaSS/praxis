/** SchedulesView/McpServersView가 공유하는 폼 스타일 상수 — 순수 추출, 시각 결과 동일. */

/** 등록 폼 텍스트 입력 공통 클래스. 너비는 호출부에서 템플릿 리터럴로 추가. */
export const inputCls =
  "h-9 px-2 rounded bg-bg border border-border text-text text-sm focus:outline-none focus:border-primary font-code";

/** 리소스 행(카드) 삭제 버튼 공통 클래스. */
export const deleteBtnCls = "text-text-muted hover:text-status-failed text-xs";
